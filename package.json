{
  "scripts": {
    "build": "tsc && parcel src/html/index.html --dist-dir build",
    "start": "parcel src/html/index.html --dist-dir build --open"
  },
  "devDependencies": {
    "eslint": "^9.23.0",
    "parcel": "^2.14.2"
  }
}
